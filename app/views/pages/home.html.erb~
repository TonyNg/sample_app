<% if signed_in? %>
  <table class="front" summary="For signed-in users">
    <tr>
      <td class="main">
        <h1 class="micropost">What's up?</h1>
        <%= render 'shared/micropost_form' %>
        <%= render 'shared/feed' %>
      </td>
      <td class="sidebar round">
     <%= render 'shared/user_info' %>
        <%= render 'shared/stats' %>
      </td>
    </tr>
  </table>
<% else %>
  <h1>Hello Cal Poly Pomona students!</h1>

  <p>
    Want to filter out the junk on Facebook and connect with fellow
    <a href="www.csupomona.edu/">Cal Poly Pomona</a>
    classmates instead? Sign up with your <b>csupomona.edu</b> email address to begin!
  </p>

  <%= link_to "Sign up now!", signup_path, :class => "signup_button round" %>
<% end %>
